license = "${license}"
modLoader="javafml"
loaderVersion="[1,)"

[[mods]]
modId = "${id}"
displayName = "${name}"
version = "${version}"
authors = "${authors}"
credits = "${contributors}"
description = "${description}"
logoFile = "icon.png"
displayURL = "${homepage_url}"
issueTrackerURL = "${issues_url}"
enumExtensions = "${enum_ext}"

[[mixins]]
config = "${id}.mixins.json"

[[accessTransformers]]
file="${at}"

${dependencies}
