@version "1";
@target "minecraft:enchantment/sweeping_edge.json";

if (rearm_config.sweepingEdge.sweepingEdgeAbility) {
    if ("minecraft:attributes" in $effects) {
        delete $effects.'minecraft:attributes';
    }
}